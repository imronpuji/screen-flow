# Screen Flow — Product Roadmap

Status flow keseluruhan. `[ ]` belum · `[~]` proses · `[x]` selesai (fungsional + user-friendly).

- [x] 1. Fondasi Electron+React+TS
- [x] 2. Izin & capture dasar (desktopCapturer + TCC probe)
- [x] 3. Simpan rekaman (MediaRecorder → temp WebM + ffmpeg export)
- [~] 4. Preview & playback — review mode + scrub/trim preview + cursor overlay + **gap-skip keep-ranges (preview ≡ export concat)**
- [~] 5. ⭐ Auto-zoom PRESISI — engine + preview + export bake + Retina/DIP mapping + anti-jitter merge/retarget + per-click enable/scale edits + **add zoom at playhead (Z)** + **focus nudge (arrow pad)**
- [~] 6. ⭐ Cursor bisa dimodifikasi — smoothing + click ring + **auto-highlight clicks** (soft pulse, toggle + persist) + size/style/hide/spotlight preview/export + **persist prefs (localStorage)**
- [~] 7. ⭐ Kamera FaceTime / webcam overlay — enumerate + live bubble + TCC + review play-sync + parallel camera.webm + export bake + free drag/snap + **mid-edge magnetic snap + 8 position presets** + corner resize (aspect lock **+ free unlock for rectangle/rounded**) + **arrow-key nudge (Shift=larger)** + **size nudge (+/-) + S/M/L presets + keys 1/2/3 + Reset (0/double-click → BR+medium)** + **`[`/`]` snap cycle + `C` shape cycle** + shapes circle/rounded/rectangle + border/shadow bake + outline color picker + mirror + opacity + A/V drift compensation + mid-recording toggle + **mic on same MediaRecorder track (export AAC, mute with camera)** + **persist overlay prefs (localStorage) + review→setup sync** + **hot-plug `devicechange` + soft inactive on track end** + **timeline active-range markers** + **review edit mute windows (Hide/Show playhead, Always on, Reset)** + **scrubber edge drag-resize** + **docked FaceTime monitor in app chrome while recording** + **chrome+review layout map (drag-to-place + scroll resize + keyboard parity + always-visible clickable snap guides + click-to-mute monitor)**; live layout bubble hidden on capture preview (no burn-in double)
- [~] 8. Background estetik — gradient/padding/rounded/shadow preview + export bake (1-frame mask) + **frame layout presets (Compact/Standard/Wide/Flat)** + corner radius slider + **persist prefs (localStorage)** + **export gradient fidelity (multi-stop + CSS angle + radial accents)**
- [~] 9. Editor ringan — trim sliders + **playhead Mark In/Out ([ ]) + cut keep-before/after (S / ⇧S)** + **multi-segment keep-ranges (X split · Delete seg · ffmpeg concat)** + **preview gap-skip (playback + scrub snap + timeline shade)** + **ripple delete toggle (merge touching clips)** + auto-zoom/cursor/background/camera toggles; trim/segments baked ke export; timeline clip markers (zoom/click seek + **camera active-range spans**); per-click zoom points (toggle + scale); **add-at-playhead (Z)**; **focus nudge**; **background frame layouts**; **camera range edit (mute windows + scrubber edge drag)**; **undo/redo (⌘Z / Ctrl+Z, coalesce 400ms)**; **collapsible property panels + hide sidebar**; magnetic timeline snapping still open
- [~] 10. Export MP4 / WebM / GIF — progress/cancel + Save As + effects bake + quality presets (Draft/Good/High) + **format picker (MP4 H.264 · WebM VP9 · GIF palette)** + **persist format+quality prefs (localStorage)**
- [~] 11. Polish user-friendly & onboarding — first-run 3-step overlay + Beautify presets + keyboard shortcuts (R/Space/E/B/Z/⌘Z/←→/Esc) + empty-state tooltips + **auto-highlight clicks** + **review Undo/Redo** + **collapsible editor chrome**
